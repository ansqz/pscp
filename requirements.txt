discord.py==2.3.2       # ใช้เชื่อมต่อ Discord API
python-dotenv==1.0.1    # สำหรับอ่าน token จากไฟล์ .env (ไม่เก็บ token ไว้ตรงๆ ในโค้ด)
asyncio                 # ใช้จัดการระบบเวลานับถอยหลัง/timeout
random                  # ใช้สุ่มคำถามและตัวเลือก (built-in แต่ถ้าอยากให้ครบก็บอกได้)
json                    # ใช้เก็บและอ่านไฟล์คำถาม/คะแนน (built-in แต่ระบุไว้เผื่อชัดเจน)
pandas==2.2.2           # จัดการข้อมูลตาราง เช่น คะแนนผู้เล่น
